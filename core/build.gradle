plugins {
  id 'mazewands.library'
}

dependencies {
}

publishing {
  publications {
    gpr(MavenPublication) {
      groupId = 'io.github.mazeneko'
      artifactId = 'mazewands-core'
      version = gitVersion()
      from components.java
    }
  }
}