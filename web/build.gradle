plugins {
  id 'mazewands.library'
}

dependencies {
  implementation project(':core')
}

publishing {
  publications {
    gpr(MavenPublication) {
      groupId = 'io.github.mazeneko'
      artifactId = 'mazewands-web'
      version = gitVersion()
      from components.java
    }
  }
}